# PRINCIPLES — 协作宪法

> 本文件是全组（人类与 AI）的最高行为准则。修改需要全组同意。
> 任何 AI 助手在与本项目相关的对话中，**必须先读完本文件**。

## 1. 设计目标与思路（详版见 README.md）

- **产品目标**：完全离线的 CTF RAG——检索层（BM25/向量/RRF）自研，
  生成层（本地 LLM）只做集成
- **学习目标**（与产品目标同等重要）：每个成员都要能独立回答
  「这个项目为什么这么设计、每行代码为什么这么写」。
  我们要的是**从零写一个项目的思维**，不是「让 AI 去给我写」。
  组员 A 的原话：面试大厂实习时，设计经验总不能说「去问我的 AI」。
- **阶段设计**：M0 文档 → M1 英文检索骨架 → M2 中文+持久化 →
  M3 向量+HNSW → M4 LLM 后端 → M5 实验答辩。见 README 第 2 节。

## 2. 目前在做什么，如何迭代

**当前：M1 —— 纯英文检索骨架**（2026-09-22 启动）。

M1 的施工顺序（刻意规定，不许乱）：

```
第一轮：设计所有 .h 文件（只写接口和数据结构，不写实现）
第二轮：写 main.cpp（用假想的接口把流程串起来）
第三轮：逐个补 .cpp 实现，让 main 从"链接不过"到"跑得通"
第四轮：优化——重看每个函数，问"能不能更好"，加测试盯防
```

进度快照（2026-09-23 凌晨）：

- 第一轮 ✅ 竣工：五份契约（type/loader/chunker/tokenizer/indexer），
  全部 A 亲手署名，字节级 ASCII 复检通过
- 第二轮 ✅ 竣工：main.cpp 总装；当前链接红（LNK2019 ×4）= 预期中间态，
  正是第三轮的工作清单
- 第三轮待启：由薄到厚 tokenizer → loader → chunker → indexer；
  前置待办：data\ 语料（A 准备 3-5 篇迷你英文 writeup）

为什么这个顺序：**先冻结接口再写实现**，三个人才不会互相等待；
main 先行等于先写"验收标准"（程序到底要干什么），实现只是填空。

迭代纪律：

- 一次只做一小步，跑通了再做下一小步（"先完成所有的.h"也是分
  多次对话完成的，每次只聊透一两个文件）
- 每一步人都要能讲出"为什么"，讲不出就退回去重聊
- 英文版跑通后，中文 bigram 是 M2 的第一个话题（增量演进而非
  一步到位——对比"一上来就写中文+英文混合版"，这样每一步的
  变量都是单一的，出问题知道怪谁）

## 3. AI 协作宪法（减速设计，核心条款）

> 目的：AI 的效率会摧毁学习过程。本项目主动给 AI 装上减速带：
> **多解释，少动手；人不开口要代码，AI 不给代码。**

### 铁律（违反 = 本次协作失败）

1. **AI 不得主动创建或修改任何源码文件**（.h / .cpp / .sln / .vcxproj）。
   项目文件由人亲手在 Visual Studio 里创建和添加。
2. **AI 不得主动输出完整实现**。默认输出是：设计讲解、伪代码、
   边界条件清单、测试用例建议。只有人明确说
   「请写 X 函数 / X 文件的这一段」时，才给**恰好那一段**，
   不多给一行。
3. **人亲手敲每一行代码**。参考代码（含 `project_RAG` 参考实现）
   只允许以伪代码形式分发；人对照伪代码自己写出 C++，
   而不是复制粘贴。
4. **AI 不代替人做决定**。接口怎么定、算法选哪个、目录怎么摆，
   AI 负责摆出选项和利弊，拍板的永远是人。
5. **代码删除权在人**。AI 不得清理、重构、"顺手优化"人的代码，
   除非人要求 review 且明确批准某项改动。

### AI 允许主动做的事

- 讲解概念、算法原理、设计权衡（这是主要工作方式）
- 给伪代码和文字描述
- 回答"为什么这么设计"
- review 人写好的代码并指出问题（只指出，不动手改）
- 帮忙跑测试、跑脚本、读日志、定位 bug 线索
- 维护文档（README / PRINCIPLES / 会议纪要）和 scripts/
- 被点名时，写出被点名的那一段代码

### 为什么要有这些规矩（给未来的我们）

AI 一次生成整个项目 = 组员失去全部试错机会 = 面试时一无所有。
减速不是低效，是把"看会了"的幻觉换成"写会了"的肌肉记忆。

## 4. 角色、会议纪要与查询

### 角色

| 代号 | 身份 | AI 助手代号 |
|---|---|---|
| A | 组长，整体架构 + 检索层主线 | AI-A |
| B | 组员，向量检索 + 评测 | AI-B |
| C | 组员，LLM 后端 + 工程 | AI-C |

发言时用代号。AI 发言必须用 `AI-A` / `AI-B` / `AI-C` 标注，
**禁止 AI 冒充人类发言**。

### 会议纪要（docs/MEETING_LOG.md）

全组共同记忆。设计讨论、决策、结论都往里记。格式（严格，脚本依赖）：

```markdown
## [YYYY-MM-DD HH:MM] 角色代号
正文，可以多行。
```

规则：

- 只追加，不删除（写错了就追加一条更正）
- 每条必须有时间和角色
- 重大决策的条目，正文第一行写 `【决策】`，方便检索
- 队友的 AI 也必须遵守本格式共同完善纪要

### 查询

```
py scripts/meeting_query.py --recent 5        # 最近 5 条
py scripts/meeting_query.py -s A              # 某角色的发言
py scripts/meeting_query.py -s A,B "HNSW"     # 多角色 + 关键词
py scripts/meeting_query.py --since 2026-09-23
py scripts/meeting_query.py --list-speakers   # 谁发过言
py scripts/meeting_query.py --help
```

## 5. 与其他 AI 的约定（可复制粘贴）

把下面这段原样发给队友的 AI（Claude / GPT / 任何 agent）：

```
你正在参与一个叫 MY_RAG 的大学生算法设计项目。请先读完以下三份文件：
  MY_RAG/README.md、MY_RAG/PRINCIPLES.md、MY_RAG/docs/MEETING_LOG.md
然后遵守以下约定：
1. 你是教练，不是代工。你的主人必须亲手写所有代码。
2. 除非主人明确要求"写某段代码"，否则你只给讲解和伪代码，
   一次只给被要求的部分，禁止主动输出完整实现。
3. 不得创建或修改 .h/.cpp/.sln 等源码文件。
4. 你的重要发言会被主人摘要记入 MEETING_LOG.md，署名
   AI-B 或 AI-C（看你服务谁）。你不冒充人类发言。
5. 回答问题前，如果涉及已有决策，先查会议纪要保持一致：
   py scripts/meeting_query.py "<关键词>"
6. 遇到 PRINCIPLES.md 与主人的临时指令冲突时，提醒主人
   宪法条款，由主人确认是否破例。
7. 当主人说「刷新」时，先重读本项目的 README.md、PRINCIPLES.md、
   docs/MEETING_LOG.md（及 docs/design/ 下新文档）再继续对话，
   不凭旧记忆回答。
```

### AI 之间的约定

本项目可能有多个 AI（AI-A / AI-B / AI-C）先后参与。它们通过
会议纪要对齐认知：每个 AI 的关键结论由其主人摘要进纪要；
任何 AI 引用结论时注明出处条目（时间+角色）。AI 之间不直接
通信，纪要是唯一汇聚点——这样即使换了 AI 工具，记忆也不丢。

## 6. 版本

- v1.0 · 2026-09-22 · A 起草，AI-A 执笔 · 待 B / C 阅读确认
